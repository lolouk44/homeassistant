div_birthday:
  has_date: true
  has_time: false
  initial: !secret birthday_div
lolo_birthday:
  has_date: true
  has_time: false
  initial: !secret birthday_lolo
#{{(as_timestamp(now()) - as_timestamp(states.input_datetime.lolo_birthday.state + " 00:00:00")) /60/60/24/365}}
